{
  "name": "centriflow-erp",
  "version": "1.0.0",
  "description": "AI-powered ERP system for education management",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai-service\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai-service": "cd ai-service && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai-service && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["erp", "education", "ai", "attendance", "analytics"],
  "author": "CentriFlow Team",
  "license": "MIT"
}
